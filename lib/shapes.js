class Shape {
  // define class using constructor to initialize/set 'color' value
  constructor(color) {
    this.color = "";
  }
  setColor(color) {
    this.color = color;
  }
}

class Circle extends Shape{
  render(){
    return ``
}